{
  "name": "medimind-server",
  "version": "1.0.0",
  "private": true,
  "description": "MediMind API - holds the Anthropic key server-side and answers assistant questions. Deployed on Railway.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.9.3"
  }
}
